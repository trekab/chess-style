<h1>Sign Up</h1>

<%= form_for(@user, url: sign_up_path) do |f| %>
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>

  <div>
    <%= f.submit "Sign up" %>
  </div>
<% end %>